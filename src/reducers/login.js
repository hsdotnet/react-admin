// 初始化state数据
const initialState = {
    loginInfo: []
}

const Login = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default Login